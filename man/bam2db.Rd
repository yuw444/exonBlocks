% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bam2db.R
\name{bam2db}
\alias{bam2db}
\title{Convert BAM to CellRanger-style matrix via SQLite}
\usage{
bam2db(
  bam_file,
  db_file,
  path_out,
  barcodes_file,
  regions_file,
  rate_cell = 1,
  rate_depth = 1,
  seed = 42,
  umi_copies_flag = 0L,
  protocol = c("10X", "zUMIs"),
  region_chr = NULL,
  region_start = 0L,
  region_end = 0L
)
}
\arguments{
\item{bam_file}{Path to BAM file (must be indexed with .bai)}

\item{db_file}{Path to output SQLite database}

\item{path_out}{Path to output directory for TSV/MTX files}

\item{barcodes_file}{Path to gzipped barcodes file (one per line)}

\item{regions_file}{Path to 8-col TSV: id, symbol, chr, start, end, annotation1, annotation2}

\item{rate_cell}{Fraction of cells to sample (0, 1]}

\item{rate_depth}{Fraction of reads to sample (0, 1]}

\item{seed}{Random seed for reproducibility}

\item{umi_copies_flag}{If 1, also output per-UMI copy counts}

\item{protocol}{Protocol type: "10X" or "zUMIs"}

\item{region_chr}{Optional chromosome for ROI filter (e.g. "chr19")}

\item{region_start}{Optional start position (1-based)}

\item{region_end}{Optional end position (1-based)}
}
\value{
NULL (invisibly). Output written to db_file and path_out.
}
\description{
Processes a BAM file, extracts cell barcodes and UMIs, maps reads to
genomic regions (genes, exons, or custom BED), and outputs a sparse
count matrix in 10x format.
}
