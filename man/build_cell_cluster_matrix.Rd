% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/build_matrix.R
\name{build_cell_cluster_matrix}
\alias{build_cell_cluster_matrix}
\title{Build Cell × Cluster Matrix from BAM}
\usage{
build_cell_cluster_matrix(
  bam,
  exon_clusters,
  out_tsv = NULL,
  out_matrix = "cell_cluster_matrix.rds",
  chr = NULL,
  xf_values = c(25L, 17L),
  tech = "10X",
  min_reads = 1
)
}
\arguments{
\item{bam}{Path to input BAM/CRAM with CB/UB tags}

\item{exon_clusters}{GRanges object with exon cluster coordinates.
Must contain columns: seqnames, ranges, strand (ranges contain start/end)}

\item{out_tsv}{Optional path for intermediate block TSV. If NULL, uses temp file.}

\item{out_matrix}{Path for output matrix RDS (dgCMatrix format)}

\item{chr}{Character vector of chromosomes to process (NULL = all)}

\item{xf_values}{Integer vector of allowed xf values (10X only)}

\item{tech}{Character: "10X" or "Smart-seq"}

\item{min_reads}{Minimum reads per cell to retain (default: 1)}
}
\value{
Invisible dgCMatrix (also written to out_matrix)
}
\description{
Creates a sparse matrix of read counts where rows are cells (CB) and
columns are exon clusters. Each entry counts how many reads from that cell
overlap that cluster.
}
\examples{
\dontrun{
mat <- build_cell_cluster_matrix(
  bam = "possorted.bam",
  exon_clusters = exon_clusters,
  out_matrix = "cell_cluster_matrix.rds"
)
}
}
