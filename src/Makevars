# Define paths to GSL and libomp libraries
# CC = /opt/homebrew/opt/llvm/bin/clang
# CXX = /opt/homebrew/opt/llvm/bin/clang++

PKG_CPPFLAGS += $(pkg-config --cflags --libs htslib)
PKG_CFLAGS += $(pkg-config --cflags --libs htslib)

PKG_LIBS += $(pkg-config --cflags --libs htslib)
